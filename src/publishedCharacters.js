// Animals published from Character Lab. Each entry's pixel data is baked in
// here as static data (exported from a developer-account draft prototype,
// then pasted in and committed) — the live game never talks to Supabase to
// render these; it's just game content, like anything else in animals.js.
//
// Shape per entry: { name, cost, speed, icon, frames } where `frames` is
// the same {idle_1, idle_2, walk_1..walk_4} object Character Lab saves.
export const PUBLISHED_ANIMALS = {
  // (empty until the first prototype is exported and pasted in)
};
